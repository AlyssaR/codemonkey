[backup]
% Options are: addns, bind, etc
general: /~, /var/www
services: addns, bind, apache

[install]
% Options are: addns, bind, etc
general: /~, /var/www
services: addns, bind, apache

[restore]
% Options are: addns, bind, etc
general: /~, /var/www
services: addns, bind, apache

[setup]
% Options are: addns, bind, etc
general: /~, /var/www
services: addns, bind, apache
